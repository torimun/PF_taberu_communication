<div class="container">
  <div class="row">
    <div class="col">
      <h3>タグ一覧ページ</h3>
    </div>
  </div>

  <div class="row">
    <% @tags.each do |all_tag|%>
      <div class="col-lg-1">
        <%= link_to customers_tag_show_path(all_tag.id) do %>
          <%= all_tag.tag %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
